'use strict';

const userController = require('./user.controller');
const instagramUrlFetcher = require('./instagramUrlFetcher.controller');

module.exports = {
    userController,
    instagramUrlFetcher
};